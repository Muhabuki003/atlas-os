// Re-export graph engine (legacy import path)
export {
  initAtlasGraph as initAtlasNodes,
  refreshAtlasGraph,
  setGraphProjects,
  highlightNode,
  focusGraphNode,
  getGraphZoom,
} from './atlasGraph.js';
